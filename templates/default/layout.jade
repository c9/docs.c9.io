mixin format_date(mtime)
  -var m_names = new Array("January", "February", "March", "April", "May", "June", "July", "August", "September",  "October", "November", "December");
  -var curr_date = mtime.getDate();
  -var sup = "";

  if (curr_date == 1 || curr_date == 21 || curr_date == 31)
   sup = "st";
  else if (curr_date == 2 || curr_date == 22)
   sup = "nd";
  else if (curr_date == 3 || curr_date == 23)
   sup = "rd";
  else
   sup = "th";
  
  -var curr_month = mtime.getMonth();
  -var curr_year = mtime.getFullYear();
  
  != m_names[curr_month] + " " + curr_date + "<sup>" + sup + "</sup>, " + curr_year 

!!! 5
html(xmlns='http://www.w3.org/1999/xhtml')
  head
    meta(http-equiv='Content-Type', content='text/html; charset=us-ascii')
    -if (whoAmI.indexOf("index.md") < 0)
      title #{pageTitle} | Cloud9 User Documentation
    -else 
      title Cloud9 IDE User Documentation
    link(href='./assets/csses/bootstrap.min.css', rel='stylesheet', type='text/css')
    link(rel="stylesheet", href="./assets/csses/font-awesome.css")
    link(href='./assets/csses/docs.css', rel='stylesheet', type='text/css')
    link(rel="stylesheet", href="https://yandex.st/highlightjs/7.0/styles/default.min.css")
    link(href="https://fonts.googleapis.com/css?family=Inconsolata", rel="stylesheet", type="text/css")
  body
    iframe(id="header_iframe", height="127px", width="100%", src="")
    .container-fluid
      .row-fluid
        .span2
          include toc
        .span10.documentationContainer
          #content
             -if (toh.length > 1)
               #toh_btn(onclick="toggleTOH(this)")
             -if (whoAmI.indexOf("index.md") < 0)
               h1 #{pageTitle}
               p.meta Last modified:&nbsp; 
                  mixin format_date(mtime)
             div
               != content
             div#disqus_thread
          #footer.wrap.pull-right
            #copyright
            #footerlinks
        .span2#tocHolder
           != headingTable(toh, 4, ['tocContainer', 'tocItem'])
      script(src='https://ajax.googleapis.com/ajax/libs/jquery/1.7.2/jquery.min.js')
      script(src='./assets/javascripts/docs.js', type='text/javascript')
      script(src="./assets/javascripts/ga.js")
      script(src="./assets/javascripts/swiftype.js")
      script(src="./assets/javascripts/disqus-ext.js")
      script(src="./assets/javascripts/iframes.js")

    iframe(id="footer_iframe", height="249px", width="100%", src="")